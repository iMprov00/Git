
***git merge --no-ff*** - это способ слить ветку с фиксированием этого факта

***git merge -s ours*** - используется для создания коммита слияния, игнорируя изменения в сливаемой ветке

# Отмена слияния в процессе
$ git merge --abort

# Откат уже завершённого слияния
$ git reset --hard HEAD~1